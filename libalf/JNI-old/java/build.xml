<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="LibALF" basedir="." default="jar">

    <property name="src.dir" value="."/>
    <property name="classes.dir" value="classes"/>
    <property name="doc.dir" value="doc"/>
    <property name="dist.dir" value="distribution"/>
    <property name="jar.name" value="libalf"/>

    <target name="clean" description="Delete all generated files">
        <delete dir="${classes.dir}" failonerror="false"/>
        <delete dir="${doc.dir}" failonerror="false"/>
        <delete dir="${dist.dir}" failonerror="false"/>
    </target>

    <target name="compile" description="Compiles LibALF">
        <mkdir dir="${classes.dir}"/>
        <javac source="1.6" srcdir="${src.dir}" destdir="${classes.dir}"/>
    </target>

    <target name="jar" description="JARs LibALF (class files only)" depends="compile">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.dir}/${jar.name}.jar" basedir="${classes.dir}"/>
    </target>

    <target name="doc" description="Generates JavaDoc">
        <mkdir dir="${doc.dir}"/>
        <javadoc sourcepath="${src.dir}" destdir="${doc.dir}"/>
    </target>

</project>

