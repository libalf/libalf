
(priority ordered)

A)

- refactor of algorithm to support SQT

- apply changes due to SQT to tests

- test structured query tree (SQT), serialization and all that stuff

- complete definition of network protocol for dispatcher (statistics, total order, STARTCOMPRESS(?), STARTTLS, ... )

B)

- total order function

- network dispatcher

C)

- in learn_regex: check/fix '+' operator in regex on command-line

- biermann algorithm


